/**
 * Send a chat message to the backend. Implement to call your advisor API.
 * New messages / typing state should be provided by the backend (e.g. streaming or polling).
 */
export async function sendChatMessage(_text: string): Promise<void> {
  // Replace with backend call, e.g. POST /api/advisor/chat with body { text }.
}
